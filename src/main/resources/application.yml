spring:
  application:
    name: librarydb
  datasource:
    url: jdbc:postgresql://192.168.5.201/library
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      #ddl-auto: update
      ddl-auto: none
    properties:
      hibernate.format_sql: true